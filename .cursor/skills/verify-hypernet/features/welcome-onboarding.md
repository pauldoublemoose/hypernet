# Welcome onboarding

First visit is a desert journey, not a CRT splash.

## Driving it

`node .cursor/skills/verify-hypernet/helpers/verify.mjs drive welcome-onboarding`

- First paint shows the digital-desert headline and no `.chrome-frame`.
- Enter advances to the HYPERNET headline, then the purpose step, then Enter the desert.
- Idle desert keeps the CRT unmounted and shows `[data-shell="dust"]`.
